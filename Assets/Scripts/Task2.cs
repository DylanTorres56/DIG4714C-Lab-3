using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Task2 : MonoBehaviour
{
    //TO DO:
    //Suppose that the cover price of a book is $X, but the bookstores
    //get a 40% discount.Shipping costs $3 for the first copy and ¢75
    //for each additional copy.Calculate the total wholescale cost and
    //profit(before operational costs) when the bookstore sells Y copies.
    //I want to enterthe X and Y values and get the cost and the profit
    //as a meaningful output.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
